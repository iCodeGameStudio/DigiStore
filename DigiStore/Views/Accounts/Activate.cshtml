@model DigiStore.Core.ViewModels.ActivateViewModel

@{
    ViewData["Title"] = "فعالسازی حساب کاربری";
    Layout = "~/Views/Shared/_Account.cshtml";
}


<h4 class="fs-6 fw-normal text-center text-secondary mb-4">فعالسازی حساب کاربری</h4>
<hr />
<form asp-action="Activate" id="Activate" method="post">
    @if (TempData["SuccessMessage"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show shadow-sm" role="alert">
            <strong>@TempData["SuccessTitle"]</strong> - @TempData["SuccessMessage"]
            <button type="button" class="close" data-dismiss="alert" aria-label="بستن">
                <span aria-hidden="true">&times;</span>
            </button>
        </div>
    }

    @if (TempData["ErrorMessage"] != null)
    {
        <div class="alert alert-danger alert-dismissible fade show shadow-sm" role="alert">
            <strong>@TempData["ErrorTitle"]</strong> - @TempData["ErrorMessage"]
            <button type="button" class="close" data-dismiss="alert" aria-label="بستن">
                <span aria-hidden="true">&times;</span>
            </button>
        </div>
    }
    <div class="row gy-2 overflow-hidden">
        <div class="col-12">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-floating mb-3">
                <input asp-for="ActiveCode" class="form-control form-control-lg persian-numbers" placeholder="کد فعالسازی 6 رقمی" />
                <span asp-validation-for="ActiveCode" class="text-danger validation-error"></span>
            </div>
        </div>
        <div class="col-12">
            <div class="d-grid my-3">
                <input type="submit" value="فعالسازی حساب" class="btn-lg btn-block btn-sumbit" />
            </div>
        </div>
    </div>
</form>


@section ScriptManager {
   
}